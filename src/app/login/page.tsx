'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function Login(){const [email,setEmail]=useState('owner@demo.local');const [password,setPassword]=useState('Owner123!');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const router=useRouter();const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok){setError(d.error||'Login failed');setBusy(false);return}router.push(d.role==='SYSTEM_OWNER'?'/owner':'/admin')};return <main className="login-shell"><form className="login-card stack" onSubmit={submit}><div><div className="brand">Bookly<span>.</span></div><h1>Welcome back</h1><p className="muted">Sign in as the System Owner or Business Admin.</p></div>{error&&<div className="alert">{error}</div>}<div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></div><button className="btn btn-primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><div className="card" style={{boxShadow:'none'}}><b>Demo credentials</b><div className="small muted" style={{marginTop:8}}>Owner: owner@demo.local / Owner123!</div><div className="small muted">Admin: admin@acme.local / Admin123!</div></div></form></main>}
